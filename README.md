# Fitcheck_SQL
Since we are using Supabase the SQL code will not be implemented in the main repository but in Supabase itself hence why this seperate repository exists to keep track of who has been creating the SQL code
